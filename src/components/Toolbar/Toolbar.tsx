import React from 'react';
import './Toolbar.css';

const Toolbar = () => {
    return (
        <header className="Toolbar">
            <h1>TURTLE PIZZA</h1>
        </header>
    );
};

export default Toolbar;